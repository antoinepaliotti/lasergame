<body>
<h1>Liste des Employés</h1>

<table>
    <tr>
        <th>Nom</th>
        <th>Centre</th>
    </tr>
    {% for employee in employees %}
        <tr>
            <td>{{ employee.username }}</td>
            <td>{{ employee.center.name }}</td>
        </tr>
    {% endfor %}
</table>

</body>